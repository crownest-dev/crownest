# Agent-use evals

Run the public client verification path with:

```bash
pnpm verify
```

The source repository also runs `pnpm check:agent-evals` before export to verify API-origin discovery, OpenAPI coverage, CLI JSON errors, MCP JSON errors, examples, and beta backup scope disclosure.
